Season column layout lives in data/key-data/season-config.lua.
Change `active` to switch which header row the party and compare views draw.
Each dungeon entry has a display name, short label, and icon FileID.
The addon does not fetch or persist live season, key, or rating data.
